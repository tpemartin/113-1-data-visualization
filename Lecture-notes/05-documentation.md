# Documentation

```diff
- Create a template of Quarto document that has title, author, and can be rendered into a Word document. The content of this document must other than content text, a code chunk to include an external image, a code chunk to read an external .csv file and display as a table. And in the content text, there is a reference to the external image and the table.
```
